{
  "name": "kavyakosh",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "vercel-build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server"
  }
}
